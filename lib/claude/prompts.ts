// 各書類種別のプロンプトはそれぞれの extractor ファイルに定義済み。
// このファイルでは共通の前書きやエラーメッセージなどを集約する。

export const GENERIC_INSTRUCTIONS = `
抽出のルール：
1. 和暦（令和・平成・昭和）は必ず西暦に変換する
2. 金額はカンマを除去し、整数の数値のみを返す
3. 判読困難・記載なしの項目は必ず null を返す（空文字ではない）
4. JSON 以外の説明文、前置き、注釈を一切含めない
5. 信頼度（_confidence）は画像の鮮明さと抽出可能性に基づき 0.0〜1.0 で返す
`;
